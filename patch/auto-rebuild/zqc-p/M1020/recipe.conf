name=Package-manager hooks that re-apply the fixes after an update
follows=cdclk-ptl and hotkeys build kernel modules, fingerprint rebuilds a library
status=verified
origin=this machine, required by the fixes physically verified on CachyOS
